# Inference + retrieval. Install torch matched to your CUDA driver first, e.g. for CUDA 12.6:
#   pip install torch==2.7.0 --index-url https://download.pytorch.org/whl/cu126
vllm==0.9.2
openai
transformers==4.53.3
faiss-cpu
datasets
jsonlines
pyarrow
sentencepiece
accelerate
hf_transfer
numpy
tqdm
